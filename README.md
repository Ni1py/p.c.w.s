# Product Catalog Project

## 🛠 Технические решения

* **Data Access Layer (DAL)**: Взаимодействие с API вынесено в изолированный слой (`lib/api.ts`). Это позволило централизованно внедрить обработку тайм-аутов через `AbortSignal.timeout(5000)` и типизировать ответы сервера, исключая ошибки исполнения при обращении к полям объектов.
* **Стратегия кэширования**: Использована встроенная система кэширования Next.js с механизмом `revalidate`. Для каталога товаров установлен интервал в 1 час, что снижает нагрузку на внешний API и ускоряет повторную загрузку страниц для пользователей.
* **State Management через URL**: Весь стейт фильтрации, поиска и пагинации синхронизирован с `searchParams`. Это обеспечивает корректную работу кнопки «Назад» в браузере и позволяет пользователям делиться ссылками на отфильтрованные результаты.
* **Отказоустойчивость Image-контента**: Реализована обработка проблемных изображений:
    * Обработчик `onError` на клиенте для динамической подстановки зашлушки, если изображение не удалось загрузить.
* **Централизованная обработка ошибок**: Использование файлов `error.tsx` и `loading.tsx` на уровне сегментов маршрутов позволило создать бесшовный UX: приложение не вылетает при сетевых сбоях, а предоставляет пользователю возможность восстановления через функцию `reset()`.
* **Темы**: Реализована поддержка темной и светлой темы. По умолчанию ставится системная.

## 📝 Допущения

* **Синхронизация фильтров и поиска**: Поскольку API DummyJSON не поддерживает одновременный серверный поиск и фильтрацию по категории, поиск имеет приоритет на уровне запроса, а фильтрация по категории применяется к результатам выборки.
* **Локализация дат**: Принято допущение использовать `en-US` формат для дат в отзывах, так как это стандарт для международных каталогов, но код подготовлен к интернационализации через `Intl`.
* **Намеренное увеличение времени разработки**: Увеличение времени разработки из-за желания довести продукт до презентабельного вида и параллельного изучения процессов (12 часов).

## 🚀 Что бы я улучшил (при наличии времени)

1. **Debounce Search**: Внедрение задержки (например, 500мс) перед отправкой поискового запроса для снижения количества сетевых запросов при вводе.
3. **Skeleton Detail**: Более детальная проработка скелетонов для сложных блоков (например, отзывов), чтобы минимизировать сдвиги макета (Layout Shift) при загрузке.
4. **Тестирование**: Добавление тестов для api, для UI.
5. **Анимации**: Добавление анимаций при перелистывании каталога и переходе на страницу с товарами.
